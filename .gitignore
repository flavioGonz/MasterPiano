node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Sólo la carpeta de datos del servidor, en la raíz: `data/` a secas también
# tapaba src/data/, que es código y sí va al repo.
/data/

# Artefactos de las sesiones de Claude (staging local, capturas, tarballs)
_to_delete/
_sync/
Claude outputs/
